torch
torchvision
Pillow
numpy
scikit-learn
matplotlib

# 웹 데모 (app/app.py)
streamlit

# 데스크톱 UI (app/app_pyqt6.py)
PyQt6>=6.5.0
qtawesome>=1.3.0

# 선별장 벨트 QA (belt_qa/pipeline.py)
ultralytics
opencv-python
lap  # BoT-SORT 트래커(ultralytics 기본)가 필요로 함. 없으면 최초 실행 시
     # ultralytics가 자동으로 pip install을 시도한다 — 오프라인이면 실패.
